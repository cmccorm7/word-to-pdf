services:
  - type: web
    name: hiring-tracker
    runtime: node
    rootDir: hiring-tracker
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: hiring-data
      mountPath: /data
      sizeGB: 1
